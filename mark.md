# 管理员功能模块技术文档

## 1. 需求模型

### 1.1 用例图说明

管理员功能模块的用例图展示了系统管理员（HOD - Head of Department）与系统之间的交互关系。用例图包含以下主要参与者：

- **管理员（Admin/HOD）**：系统的主要使用者，负责整个学生管理系统的运营和管理

用例图展示了管理员可以执行的核心功能用例，包括：

1. **用户管理用例**：管理教师和学生的账户信息
2. **课程管理用例**：管理专业、科目和学期信息
3. **出勤管理用例**：查看学生出勤记录
4. **反馈管理用例**：处理学生和教师的反馈信息
5. **请假管理用例**：审批学生和教师的请假申请
6. **通知管理用例**：向学生和教师发送系统通知
7. **活动管理用例**：审批和管理校园活动
8. **系统监控用例**：查看系统整体统计信息

每个用例都通过关联关系与管理员参与者连接，表示管理员可以执行这些操作。用例之间的关系通过包含（include）和扩展（extend）关系来描述，例如"查看出勤记录"用例可能包含"选择科目"和"选择学期"等子用例。

### 1.2 功能需求列表

#### 1.2.1 用户管理功能

**FR-ADM-001：教师账户管理**
- **描述**：管理员可以添加、编辑、删除和查看教师账户信息
- **详细需求**：
  - 添加教师时需提供：姓名、邮箱、密码、性别、联系电话、头像照片
  - 编辑教师信息时支持修改所有字段，密码为可选更新项
  - 删除教师前需检查是否有关联的科目，如有则提示无法删除
  - 支持按姓名、邮箱搜索教师
  - 支持分页显示，每页显示10条记录

**FR-ADM-002：学生账户管理**
- **描述**：管理员可以添加、编辑、删除和查看学生账户信息
- **详细需求**：
  - 添加学生时需提供：姓名、邮箱、密码、性别、联系电话、头像照片、所属专业、所属学期
  - 编辑学生信息时支持修改所有字段，密码为可选更新项
  - 删除学生前需自动清理相关的出勤记录
  - 支持按姓名、邮箱、专业搜索学生
  - 支持分页显示，每页显示10条记录

**FR-ADM-003：管理员个人信息管理**
- **描述**：管理员可以查看和编辑自己的个人信息
- **详细需求**：
  - 支持修改姓名、密码、头像
  - 密码修改为可选操作

#### 1.2.2 课程管理功能

**FR-ADM-004：专业管理**
- **描述**：管理员可以添加、编辑、删除和查看专业信息
- **详细需求**：
  - 专业信息包括：专业名称
  - 删除专业前需检查是否有学生关联，如有则提示无法删除

**FR-ADM-005：科目管理**
- **描述**：管理员可以添加、编辑、删除和查看科目信息
- **详细需求**：
  - 科目信息包括：科目名称、所属专业、授课教师
  - 支持查看所有科目的列表，显示科目名称、专业和教师信息

**FR-ADM-006：学期管理**
- **描述**：管理员可以添加、编辑、删除和查看学期信息
- **详细需求**：
  - 学期信息包括：开始日期、结束日期
  - 删除学期前需检查是否有学生关联，如有则提示无法删除

#### 1.2.3 出勤管理功能

**FR-ADM-007：查看学生出勤记录**
- **描述**：管理员可以查看学生的出勤情况
- **详细需求**：
  - 支持按科目和学期筛选出勤记录
  - 显示选定日期和科目的学生出勤状态
  - 以JSON格式返回出勤数据，包含学生姓名和出勤状态

#### 1.2.4 反馈管理功能

**FR-ADM-008：学生反馈管理**
- **描述**：管理员可以查看和回复学生的反馈信息
- **详细需求**：
  - 显示所有学生的反馈列表，包括反馈内容、提交时间
  - 支持对每条反馈进行回复
  - 回复后保存到数据库

**FR-ADM-009：教师反馈管理**
- **描述**：管理员可以查看和回复教师的反馈信息
- **详细需求**：
  - 显示所有教师的反馈列表，包括反馈内容、提交时间
  - 支持对每条反馈进行回复
  - 回复后保存到数据库

#### 1.2.5 请假管理功能

**FR-ADM-010：学生请假审批**
- **描述**：管理员可以查看和审批学生的请假申请
- **详细需求**：
  - 显示所有学生的请假申请列表，按创建时间倒序排列
  - 显示请假日期、请假原因、申请状态
  - 支持批准（状态设为1）或拒绝（状态设为-1）请假申请
  - 审批操作通过AJAX异步提交

**FR-ADM-011：教师请假审批**
- **描述**：管理员可以查看和审批教师的请假申请
- **详细需求**：
  - 显示所有教师的请假申请列表，按创建时间倒序排列
  - 显示请假日期、请假原因、申请状态
  - 支持批准（状态设为1）或拒绝（状态设为-1）请假申请
  - 审批操作通过AJAX异步提交

#### 1.2.6 通知管理功能

**FR-ADM-012：向学生发送通知**
- **描述**：管理员可以向学生发送系统通知
- **详细需求**：
  - 显示所有学生列表供选择
  - 支持选择特定学生发送通知
  - 通知通过Firebase Cloud Messaging (FCM)推送
  - 通知内容保存到数据库

**FR-ADM-013：向教师发送通知**
- **描述**：管理员可以向教师发送系统通知
- **详细需求**：
  - 显示所有教师列表供选择
  - 支持选择特定教师发送通知
  - 通知通过Firebase Cloud Messaging (FCM)推送
  - 通知内容保存到数据库

#### 1.2.7 活动管理功能

**FR-ADM-014：活动审批管理**
- **描述**：管理员可以查看和管理所有校园活动
- **详细需求**：
  - 显示所有活动的列表，包括待审批、已通过、已拒绝的活动
  - 支持按活动标题、描述搜索活动
  - 支持按审批状态筛选活动
  - 支持按开始时间或结束时间排序
  - 支持分页显示，每页显示10条记录
  - 显示活动的基本信息：标题、描述、发起教师、地点、时间、参与人数限制、审批状态

**FR-ADM-015：活动审批**
- **描述**：管理员可以审批教师发起的校园活动
- **详细需求**：
  - 显示活动的详细信息
  - 支持批准（状态设为1）或拒绝（状态设为2）活动
  - 支持添加审批意见或说明（admin_reply字段）
  - 审批后更新活动状态并保存

#### 1.2.8 系统监控功能

**FR-ADM-016：系统仪表板**
- **描述**：管理员可以查看系统的整体统计信息
- **详细需求**：
  - 显示总学生数、总教师数、总专业数、总科目数
  - 显示各科目的出勤统计图表
  - 以图表形式展示数据，便于直观了解系统运行状况

#### 1.2.9 系统功能

**FR-ADM-017：邮箱唯一性验证**
- **描述**：系统在添加或编辑用户时验证邮箱的唯一性
- **详细需求**：
  - 添加用户时检查邮箱是否已存在
  - 编辑用户时检查新邮箱是否与其他用户冲突
  - 通过AJAX异步验证邮箱可用性

**FR-ADM-018：多角色并行登录支持**
- **描述**：系统支持管理员在同一浏览器中与其他角色（教师、学生）并行登录
- **详细需求**：
  - 管理员登录状态独立存储，不影响其他角色
  - 使用独立的session key（admin_user_id）存储管理员登录状态
  - 中间件自动识别URL路径并加载对应角色的用户信息

### 1.3 非功能性需求

#### 1.3.1 性能需求

**NFR-ADM-001：响应时间**
- 页面加载时间应在3秒内完成
- 数据库查询操作应在1秒内完成
- AJAX异步请求响应时间应在500毫秒内

**NFR-ADM-002：并发处理**
- 系统应支持至少50个管理员同时在线操作
- 数据库查询应使用select_related和prefetch_related优化，避免N+1查询问题

**NFR-ADM-003：数据分页**
- 列表页面应支持分页显示，每页默认显示10条记录
- 分页应支持快速跳转和页码导航

#### 1.3.2 安全性需求

**NFR-ADM-004：身份认证**
- 管理员必须通过邮箱和密码登录
- 登录状态通过session管理，支持多角色并行登录
- 未登录的管理员无法访问管理员功能页面

**NFR-ADM-005：权限控制**
- 管理员只能访问管理员功能模块的URL路径
- 通过中间件（LoginCheckMiddleWare）进行权限验证
- URL路径以`/admin/`或`/admin_`开头的页面需要管理员权限

**NFR-ADM-006：数据验证**
- 所有用户输入必须进行服务器端验证
- 邮箱格式验证和唯一性检查
- 文件上传（头像）需要进行类型和大小验证
- 密码存储使用Django的密码哈希机制

**NFR-ADM-007：CSRF保护**
- 所有POST请求必须包含CSRF令牌
- 使用Django的CSRF中间件进行保护

#### 1.3.3 可用性需求

**NFR-ADM-008：用户界面**
- 界面应清晰直观，使用AdminLTE模板框架
- 支持响应式设计，适配不同屏幕尺寸
- 操作反馈及时，成功和错误消息明确

**NFR-ADM-009：错误处理**
- 系统应提供友好的错误提示信息
- 异常情况应记录日志，不影响系统正常运行
- 删除操作前应检查关联数据，提供明确的错误提示

**NFR-ADM-010：搜索和筛选**
- 支持按多个字段搜索（姓名、邮箱等）
- 支持按状态筛选数据
- 搜索结果实时更新

#### 1.3.4 可维护性需求

**NFR-ADM-011：代码组织**
- 管理员相关视图函数集中在`hod_views.py`文件中
- 使用Django表单类（forms.py）进行数据验证和处理
- 模型定义集中在`models.py`文件中

**NFR-ADM-012：日志记录**
- 关键操作应记录日志
- 错误和异常应记录详细信息

#### 1.3.5 可扩展性需求

**NFR-ADM-013：模块化设计**
- 功能模块相对独立，便于扩展
- 使用Django的MVT（Model-View-Template）架构模式
- 支持未来添加新的管理功能

## 2. 软件设计模型

### 2.1 类图说明

管理员功能模块的类图展示了系统中核心类及其之间的关系。类图主要包含以下类：

#### 2.1.1 用户相关类

**CustomUser类**
- 继承自Django的AbstractUser，是系统的用户基类
- 主要属性：email（邮箱，唯一）、user_type（用户类型：1=管理员，2=教师，3=学生）、gender（性别）、profile_pic（头像）、phone_number（联系电话）、fcm_token（Firebase推送令牌）
- 与Admin、Staff、Student类存在一对一关系

**Admin类**
- 管理员模型类，与CustomUser存在一对一关系（OneToOneField）
- 通过CASCADE级联删除，删除CustomUser时自动删除Admin记录

#### 2.1.2 课程相关类

**Course类**
- 专业模型类
- 主要属性：name（专业名称）、created_at、updated_at
- 与Student、Subject类存在一对多关系

**Subject类**
- 科目模型类
- 主要属性：name（科目名称）、created_at、updated_at
- 与Staff存在多对一关系（ForeignKey）
- 与Course存在多对一关系（ForeignKey）
- 与Attendance、StudentResult存在一对多关系

**Session类**
- 学期模型类
- 主要属性：start_year（开始日期）、end_year（结束日期）
- 与Student、Attendance存在一对多关系

#### 2.1.3 学生和教师相关类

**Student类**
- 学生模型类，与CustomUser存在一对一关系
- 主要属性：course（所属专业，ForeignKey）、session（所属学期，ForeignKey）
- 与多个模型存在一对多关系：LeaveReportStudent、FeedbackStudent、NotificationStudent、StudentResult、ActivityRegistration等

**Staff类**
- 教师模型类，与CustomUser存在一对一关系
- 与Subject、LeaveReportStaff、FeedbackStaff、NotificationStaff、Activity等存在一对多关系

#### 2.1.4 出勤相关类

**Attendance类**
- 出勤记录模型类
- 主要属性：session（学期）、subject（科目）、date（日期）
- 与AttendanceReport存在一对多关系

**AttendanceReport类**
- 学生出勤报告模型类
- 主要属性：student（学生）、attendance（出勤记录）、status（出勤状态：True/False）

#### 2.1.5 反馈和请假相关类

**FeedbackStudent类**
- 学生反馈模型类
- 主要属性：student（学生）、feedback（反馈内容）、reply（管理员回复）

**FeedbackStaff类**
- 教师反馈模型类
- 主要属性：staff（教师）、feedback（反馈内容）、reply（管理员回复）

**LeaveReportStudent类**
- 学生请假报告模型类
- 主要属性：student（学生）、date（请假日期）、message（请假原因）、status（审批状态：0=待审批，1=已批准，-1=已拒绝）

**LeaveReportStaff类**
- 教师请假报告模型类
- 主要属性：staff（教师）、date（请假日期）、message（请假原因）、status（审批状态）

#### 2.1.6 通知相关类

**NotificationStudent类**
- 学生通知模型类
- 主要属性：student（学生）、message（通知内容）

**NotificationStaff类**
- 教师通知模型类
- 主要属性：staff（教师）、message（通知内容）

#### 2.1.7 活动相关类

**Activity类**
- 校园活动模型类
- 主要属性：title（活动标题）、description（活动描述）、organizer（发起教师）、location（活动地点）、start_time（开始时间）、end_time（结束时间）、max_participants（最大参与人数）、status（审批状态：0=待审批，1=已通过，2=已拒绝）、admin_reply（管理员回复）
- 与ActivityRegistration、ActivityAttendance、ActivityFeedback存在一对多关系

**ActivityRegistration类**
- 活动报名模型类
- 主要属性：activity（活动）、student（学生）、registered_at（报名时间）
- 通过unique_together确保每个学生只能报名一次

**ActivityAttendance类**
- 活动签到模型类
- 主要属性：activity（活动）、student（学生）、is_present（是否出席）、checked_by（签到确认人）、checked_at（签到时间）

**ActivityFeedback类**
- 活动评价模型类
- 主要属性：activity（活动）、student（学生）、rating（评分：1-5星）、comment（评价内容）

#### 2.1.8 表单类

**StaffForm类**
- 教师表单类，继承自CustomUserForm
- 用于添加和编辑教师信息

**StudentForm类**
- 学生表单类，继承自CustomUserForm
- 额外包含course和session字段

**CourseForm类**
- 专业表单类
- 包含name字段

**SubjectForm类**
- 科目表单类
- 包含name、staff、course字段

**SessionForm类**
- 学期表单类
- 包含start_year和end_year字段

**ActivityApprovalForm类**
- 活动审批表单类
- 包含status和admin_reply字段

#### 2.1.9 关系说明

类图中的关系类型包括：
- **一对一关系（OneToOneField）**：CustomUser与Admin、Staff、Student之间的关系
- **一对多关系（ForeignKey）**：Course与Student、Subject之间的关系；Staff与Subject之间的关系等
- **多对多关系**：通过中间表实现，如ActivityRegistration连接Activity和Student
- **继承关系**：表单类之间的继承关系，如StaffForm继承CustomUserForm

类图清晰地展示了管理员模块中所有数据模型的结构和相互关系，为系统实现提供了清晰的数据结构设计。

### 2.2 交互图说明

交互图展示了管理员功能模块中典型业务流程的交互过程。主要包括以下交互场景：

#### 2.2.1 管理员登录交互流程

**参与者**：管理员用户、浏览器、Django视图层、认证中间件、数据库

**交互步骤**：
1. 管理员在登录页面输入邮箱和密码，提交登录表单
2. 浏览器发送POST请求到`/d