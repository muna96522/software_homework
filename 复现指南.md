# 学生管理系统 - 项目复现指南

## 📋 项目简介

这是一个基于 Django 3.1.1 开发的学生管理系统，支持管理员、教师和学生三种角色，包含学生管理、课程管理、考勤管理、成绩管理等功能。

## 🔧 环境要求

- **Python**: 3.7 或更高版本
- **操作系统**: Windows / macOS / Linux
- **数据库**: SQLite3 (默认，已包含在项目中)

## 📦 复现步骤

### 1. 克隆或下载项目

如果项目已经在本地，跳过此步。否则：

```bash
git clone https://github.com/jobic10/student-management-using-django.git
cd student-management-using-django
```

### 2. 创建虚拟环境（推荐）

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

主要依赖包括：
- Django==3.1.1
- Pillow==7.2.0 (用于图片处理)
- 其他依赖见 `requirements.txt`

### 4. 数据库设置

项目默认使用 SQLite3 数据库，数据库文件 `db.sqlite3` 已包含在项目中。

**如果数据库文件存在：**
- 直接使用现有数据库，无需迁移

**如果需要重新初始化数据库：**
```bash
# 删除旧数据库（可选）
del db.sqlite3  # Windows
# 或
rm db.sqlite3   # macOS/Linux

# 执行数据库迁移
python manage.py makemigrations
python manage.py migrate
```

### 5. 创建超级用户（管理员）

```bash
python manage.py createsuperuser
```

按提示输入：
- 邮箱（作为用户名）
- 密码
- 确认密码

### 6. 生成测试数据（可选）

项目提供了测试数据生成脚本：

```bash
python generate_test_data.py
```

这会生成：
- 多个管理员账户
- 多个教师账户
- 多个学生账户
- 课程、科目、学期等数据

### 7. 查看登录信息（如果使用了测试数据）

```bash
python show_login_info.py
```

这会显示所有测试账户的登录邮箱和密码。

### 8. 运行开发服务器

**Windows:**
```bash
python manage.py runserver
```

**macOS/Linux:**
```bash
python3 manage.py runserver
```

或者使用项目提供的便捷脚本：

**Windows:**
```bash
runserver.bat
```

**PowerShell:**
```bash
.\runserver.ps1
```

服务器启动后，访问：**http://127.0.0.1:8000/**

## 🔐 默认登录账户

根据 README.md，项目可能包含以下默认账户：

### 管理员 (HOD/SuperAdmin)
- **邮箱**: admin@admin.com
- **密码**: admin

### 教师 (Staff)
- **邮箱**: staff@staff.com
- **密码**: staff

### 学生 (Student)
- **邮箱**: student@student.com
- **密码**: student

> ⚠️ **注意**: 如果这些账户不存在，请先创建超级用户或运行测试数据生成脚本。

## 🎯 功能特性

### 管理员功能
- 查看整体统计图表
- 管理教师、学生、课程、科目、学期
- 查看学生考勤
- 审核请假申请
- 回复反馈

### 教师功能
- 查看相关统计图表
- 记录/更新学生考勤
- 添加/更新学生成绩
- 申请请假
- 发送反馈给管理员

### 学生功能
- 查看个人统计图表
- 查看考勤记录
- 查看成绩
- 申请请假
- 发送反馈

## 🛠️ 常见问题

### 1. 端口被占用

如果 8000 端口被占用，可以指定其他端口：

```bash
python manage.py runserver 8080
```

### 2. 静态文件无法加载

确保 `settings.py` 中的静态文件配置正确。开发环境下通常不需要额外配置。

### 3. 数据库迁移错误

如果遇到迁移错误，可以尝试：

```bash
python manage.py migrate --run-syncdb
```

### 4. 图片上传功能

确保 `media` 文件夹存在且有写入权限。项目已包含 `media` 文件夹。

### 5. 邮件功能配置（可选）

如果需要使用邮件功能（如密码重置），需要在 `settings.py` 中配置：

```python
EMAIL_HOST_USER = os.environ.get('EMAIL_ADDRESS')
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_PASSWORD')
```

设置环境变量或在 `settings.py` 中直接配置。

## 📁 项目结构

```
student-management-using-django/
├── manage.py                 # Django 管理脚本
├── requirements.txt          # 项目依赖
├── db.sqlite3               # SQLite 数据库文件
├── main_app/                # 主应用
│   ├── models.py            # 数据模型
│   ├── views.py             # 视图函数
│   ├── urls.py              # URL 路由
│   ├── templates/           # 模板文件
│   └── static/              # 静态文件
├── student_management_system/  # 项目配置
│   ├── settings.py          # 项目设置
│   └── urls.py              # 根 URL 配置
└── media/                   # 媒体文件（上传的图片等）
```

## 🔍 查看数据库内容

### 方法1：使用提供的脚本
```bash
python check_database.py
```

### 方法2：使用 Django Admin
访问：http://127.0.0.1:8000/admin/

### 方法3：使用 Django Shell
```bash
python manage.py shell
```

## 📝 注意事项

1. **安全设置**: 生产环境请修改 `settings.py` 中的 `SECRET_KEY` 和 `ALLOWED_HOSTS`
2. **DEBUG模式**: 开发环境 `DEBUG=True`，生产环境请设置为 `False`
3. **数据库备份**: 定期备份 `db.sqlite3` 文件
4. **虚拟环境**: 建议始终在虚拟环境中运行项目

## 🚀 快速启动（一键运行）

如果项目已配置好，可以直接运行：

**Windows:**
```bash
runserver.bat
```

**PowerShell:**
```bash
.\runserver.ps1
```

**手动启动:**
```bash
python manage.py runserver
```

然后访问：**http://127.0.0.1:8000/**

## 📞 获取帮助

- 查看项目 README.md
- 查看 `查看数据库说明.md` 了解数据库相关信息
- 查看 Django 官方文档：https://docs.djangoproject.com/

---

**祝您使用愉快！** 🎉

