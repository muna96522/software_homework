# 多角色并行登录功能说明

## 功能概述

本系统已实现多角色并行登录功能，允许在同一浏览器（或同一浏览器的不同标签页）中同时保持三个角色（管理员、教师、学生）的登录状态，各角色间的操作互不干扰。

## 核心实现原理

### 1. 独立的Session存储

系统为每个角色使用独立的session key来存储登录状态：
- 管理员：`admin_user_id`
- 教师：`teacher_user_id`
- 学生：`student_user_id`

### 2. 智能中间件

系统使用两个中间件来实现多角色认证：

1. **`MultiRoleAuthMiddleware`**：在`process_request`中根据URL路径从对应角色的session中获取用户，并设置到`request.user`，覆盖Django默认的认证用户。这确保视图函数访问`request.user`时得到的是对应角色的用户。

2. **`LoginCheckMiddleWare`**：在`process_view`中检查用户是否有权限访问对应的页面，如果对应角色未登录则重定向到登录页面。

### 3. 角色特定的登录/登出

- **登录**：每个角色使用独立的登录函数，将用户ID存储到对应角色的session中
- **登出**：支持按角色单独登出，或一次性登出所有角色

## 使用方法

### 登录多个角色

1. 打开登录页面：`http://127.0.0.1:8000/`
2. 选择"管理员"角色，输入管理员账号密码，登录
3. 在同一浏览器的**新标签页**中再次打开登录页面
4. 选择"教师"角色，输入教师账号密码，登录
5. 可以继续登录"学生"角色

### 访问不同角色的页面

登录后，可以在不同标签页中同时访问：
- 管理员页面：`http://127.0.0.1:8000/admin/home/`
- 教师页面：`http://127.0.0.1:8000/staff/home/`
- 学生页面：`http://127.0.0.1:8000/student/home/`

每个标签页会显示对应角色的界面，互不干扰。

### 登出

#### 方式1：按角色登出（推荐）

访问以下URL可以登出特定角色：
- 登出管理员：`http://127.0.0.1:8000/logout-role/admin/`
- 登出教师：`http://127.0.0.1:8000/logout-role/teacher/`
- 登出学生：`http://127.0.0.1:8000/logout-role/student/`

登出后，如果其他角色已登录，会自动重定向到其他角色的首页。

#### 方式2：登出所有角色

访问：`http://127.0.0.1:8000/logout_user/`

会清除所有角色的登录状态。

## 技术细节

### 核心模块

1. **`main_app/multi_role_auth.py`**
   - 提供多角色session管理的工具函数
   - 包括：`set_user_to_role_session()`, `get_user_from_role_session()`, `remove_user_from_role_session()` 等

2. **`main_app/middleware.py`**
   - `LoginCheckMiddleWare` 中间件
   - 根据URL路径自动识别需要的角色
   - 从对应角色的session中获取用户并设置到 `request.user`

3. **`main_app/views.py`**
   - 修改了 `do_admin_login()`, `do_teacher_login()`, `do_student_login()` 函数
   - 新增 `logout_role()` 函数支持按角色登出
   - 修改 `logout_user()` 函数支持登出所有角色

### URL路由

新增路由：
```python
path("logout-role/<str:role>/", views.logout_role, name='logout_role'),
```

## 注意事项

1. **同一浏览器会话**：多角色并行登录功能基于同一浏览器的session，不同浏览器之间是独立的。

2. **标签页共享**：同一浏览器的不同标签页共享session，所以可以同时保持多个角色的登录状态。

3. **权限检查**：中间件会自动检查访问的页面是否需要对应角色的登录状态，如果未登录会重定向到登录页面。

4. **向后兼容**：保留了原有的 `logout_user` 函数，可以一次性登出所有角色。

## 测试建议

1. 测试同时登录三个角色
2. 测试在不同标签页访问不同角色的页面
3. 测试按角色单独登出
4. 测试登出所有角色
5. 测试未登录角色访问对应页面时的重定向

## 常见问题

**Q: 为什么我登录了一个角色后，再登录另一个角色会覆盖？**
A: 不会覆盖。每个角色使用独立的session key，互不影响。如果遇到问题，请检查中间件是否正确配置。

**Q: 如何在前端显示当前登录的角色？**
A: 在模板中可以直接使用 `{{ request.user }}` 来获取当前角色的用户信息。

**Q: 可以同时登录同一个账号的不同角色吗？**
A: 可以，只要该账号在数据库中有对应的角色记录。例如，如果同一个邮箱既是管理员又是教师，可以同时登录这两个角色。

