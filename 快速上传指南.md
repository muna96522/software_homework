# 快速上传指南 - 三人分工

## 🎯 谢树荟（系统架构）

### 必须上传的文件：

```
student-management-using-django/
├── main_app/
│   ├── views.py                    # 登录相关函数
│   ├── EmailBackend.py             # 邮箱认证
│   ├── multi_role_auth.py          # 多角色认证
│   ├── middleware.py                # 权限中间件
│   ├── urls.py                      # URL路由（完整）
│   └── templates/
│       ├── main_app/                # 公共模板（整个目录）
│       └── registration/            # 注册模板（整个目录）
├── student_management_system/
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   ├── asgi.py
│   └── __init__.py
├── manage.py
├── requirements.txt
└── README.md
```

**核心文件数量**：约15-20个文件/目录

---

## 🎯 黄佳蓉（管理员 + 教师模块）

### 必须上传的文件：

```
student-management-using-django/
├── main_app/
│   ├── hod_views.py                 # 管理员视图（完整）
│   ├── staff_views.py               # 教师视图（完整）
│   ├── models.py                    # 数据模型（包含活动模型）
│   ├── forms.py                     # 表单（包含活动表单）
│   ├── utils.py                     # 工具函数
│   ├── EditResultView.py            # 成绩编辑视图
│   ├── migrations/
│   │   └── 0003_activity_*.py       # 活动模型迁移
│   └── templates/
│       ├── hod_template/            # 管理员模板（26个文件）
│       └── staff_template/           # 教师模板（13个文件）
└── requirements.txt
```

**核心文件数量**：约40-45个文件

**模板文件清单**：
- 管理员模板（26个）：home_content.html, add_staff_template.html, manage_staff.html, add_student_template.html, manage_student.html, add_course_template.html, manage_course.html, add_subject_template.html, manage_subject.html, add_session_template.html, manage_session.html, admin_view_attendance.html, admin_view_profile.html, student_feedback_template.html, staff_feedback_template.html, student_leave_view.html, staff_leave_view.html, student_notification.html, staff_notification.html, **manage_activities.html**, **approve_activity.html**, 以及所有edit_*_template.html文件
- 教师模板（13个）：home_content.html, staff_take_attendance.html, staff_update_attendance.html, staff_add_result.html, edit_student_result.html, staff_apply_leave.html, staff_feedback.html, staff_view_profile.html, staff_view_notification.html, **create_activity.html**, **my_activities.html**, **view_registrations.html**, **check_attendance.html**

---

## 🎯 易晶（学生模块）

### 必须上传的文件：

```
student-management-using-django/
├── main_app/
│   ├── student_views.py             # 学生视图（完整）
│   ├── models.py                    # 数据模型（包含活动模型）
│   ├── forms.py                     # 表单（包含活动表单）
│   ├── migrations/
│   │   └── 0003_activity_*.py       # 活动模型迁移
│   └── templates/
│       └── student_template/        # 学生模板（10个文件）
└── requirements.txt
```

**核心文件数量**：约15-20个文件

**模板文件清单**（10个）：
- home_content.html
- student_view_attendance.html
- student_view_result.html
- student_apply_leave.html
- student_feedback.html
- student_view_profile.html
- student_view_notification.html
- **view_activities.html**（活动管理）
- **my_activities.html**（活动管理）
- **feedback_activity.html**（活动管理）

---

## ⚠️ 重要提示

### 1. 共享文件说明
- **models.py**：三个人都需要，但各自负责的模型不同
- **forms.py**：三个人都需要，但各自负责的表单不同
- **urls.py**：谢树荟需要完整文件，其他人只需要相关路由部分

### 2. 活动管理功能分工
- **黄佳蓉**：管理员活动审批（`admin_manage_activities`, `admin_approve_activity`）+ 教师活动创建和管理（`staff_create_activity`, `staff_my_activities`, `staff_view_registrations`, `staff_check_attendance`）
- **易晶**：学生活动浏览和参与（`student_view_activities`, `student_register_activity`, `student_my_activities`, `student_feedback_activity`）

### 3. 上传前检查
- [ ] 代码无语法错误
- [ ] 模板路径正确
- [ ] 已排除敏感信息（密码、密钥）
- [ ] 已配置.gitignore（排除db.sqlite3, __pycache__等）

---

## 📦 推荐上传方式

### 方式一：三个独立仓库（最简单）
- 谢树荟：创建 `student-management-core` 仓库
- 黄佳蓉：创建 `student-management-admin-staff` 仓库
- 易晶：创建 `student-management-student` 仓库

### 方式二：一个仓库 + 三个分支
- `main` 分支：完整项目（谢树荟维护）
- `admin-staff` 分支：管理员和教师模块（黄佳蓉）
- `student` 分支：学生模块（易晶）

### 方式三：一个仓库 + 三个目录
```
student-management-using-django/
├── core/          # 谢树荟
├── admin-staff/   # 黄佳蓉
└── student/       # 易晶
```

---

**生成时间**：2025年12月

