# 数据库数据查看方式

## 1. 数据库文件位置

**主要数据文件：**
- 📁 `db.sqlite3` - SQLite数据库文件（项目根目录）
  - 路径：`D:\GitHub\student-management-using-django\db.sqlite3`
  - 这是所有数据的存储位置

## 2. 查看数据的方法

### 方法1：使用Python脚本（推荐）
我已经为您创建了 `check_database.py` 脚本，可以直接运行：

```bash
python check_database.py
```

这个脚本会显示所有数据的统计信息。

### 方法2：使用Django Shell
```bash
python manage.py shell
```

然后在shell中输入：
```python
from main_app.models import *
# 查看所有用户
CustomUser.objects.all()
# 查看所有学生
Student.objects.all()
# 查看所有教师
Staff.objects.all()
# 查看所有专业
Course.objects.all()
```

### 方法3：使用Django Admin后台
访问：`http://127.0.0.1:8000/admin/`
- 需要先创建超级用户：`python manage.py createsuperuser`
- 然后登录查看所有模型数据

### 方法4：使用SQLite浏览器工具
可以使用以下工具直接打开 `db.sqlite3` 文件：
- **DB Browser for SQLite** (免费，推荐)
  - 下载：https://sqlitebrowser.org/
  - 打开 `db.sqlite3` 文件即可查看所有表和数据
  
- **VS Code扩展**
  - 安装 "SQLite Viewer" 或 "SQLite" 扩展
  - 直接在VS Code中打开 `db.sqlite3` 文件

- **在线工具**
  - https://sqliteviewer.app/
  - 上传 `db.sqlite3` 文件即可查看

### 方法5：使用命令行工具
```bash
# 安装sqlite3命令行工具（Windows需要单独安装）
sqlite3 db.sqlite3

# 在sqlite3中执行：
.tables          # 查看所有表
SELECT * FROM main_app_customuser;  # 查看用户表
.schema          # 查看表结构
.quit            # 退出
```

## 3. 当前数据库状态

根据刚才的查询，您的数据库包含：
- ✅ 10个用户（3个管理员，4个教师，3个学生）
- ✅ 5个专业
- ✅ 1个学期
- ✅ 1个科目
- ⚠️ 暂无业务数据（出勤、请假、反馈、成绩等）

## 4. 日志文件

**注意：** 当前项目没有配置日志系统，所以没有日志文件。

如果需要添加日志功能，可以在 `settings.py` 中添加日志配置。

## 5. 数据备份

建议定期备份 `db.sqlite3` 文件：
```bash
# Windows
copy db.sqlite3 db_backup_20251217.sqlite3

# 或者使用Python脚本自动备份
```

