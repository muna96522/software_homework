# 管理员模块 - 软件设计模型文档

## 1. 概述

本文档描述了管理员模块的软件设计模型，包括类图、交互图等设计制品，用于说明系统的架构设计和关键业务流程的实现方式。

## 2. 类图

> **说明**：此处应插入管理员模块的类图。类图应展示以下关键类及其关系：
> - **视图类**：`hod_views.py`中的各个视图函数（如`admin_home`、`add_staff`、`manage_staff`等）
> - **模型类**：`models.py`中的相关数据模型（`CustomUser`、`Admin`、`Staff`、`Student`、`Course`、`Subject`、`Session`、`Activity`等）
> - **表单类**：`forms.py`中的表单类（`StaffForm`、`StudentForm`、`CourseForm`等）
> - **工具类**：`utils.py`中的工具函数（`paginate_queryset`、`search_students`、`search_staff`等）
> - **关系说明**：展示类之间的继承、关联、依赖等关系

### 2.1 核心类说明

#### 2.1.1 视图层（View Layer）
- **admin_home**：管理员首页视图，负责统计信息展示
- **add_staff / edit_staff / manage_staff / delete_staff**：教师管理相关视图
- **add_student / edit_student / manage_student / delete_student**：学生管理相关视图
- **add_course / edit_course / manage_course / delete_course**：课程管理相关视图
- **add_subject / edit_subject / manage_subject / delete_subject**：科目管理相关视图
- **add_session / edit_session / manage_session / delete_session**：学期管理相关视图
- **admin_view_attendance**：出勤查看视图
- **student_feedback_message / staff_feedback_message**：反馈处理视图
- **view_student_leave / view_staff_leave**：请假审批视图
- **admin_notify_student / admin_notify_staff**：通知管理视图
- **admin_manage_activities / admin_approve_activity**：活动管理视图
- **admin_view_profile**：个人资料管理视图

#### 2.1.2 模型层（Model Layer）
- **CustomUser**：用户基类，继承自Django的AbstractUser，包含用户的基本信息
- **Admin**：管理员模型，与CustomUser一对一关联
- **Staff**：教师模型，与CustomUser一对一关联
- **Student**：学生模型，与CustomUser一对一关联，关联Course和Session
- **Course**：课程模型
- **Subject**：科目模型，关联Staff和Course
- **Session**：学期模型
- **Activity**：活动模型，关联Staff（组织者）
- **Attendance / AttendanceReport**：出勤相关模型
- **LeaveReportStudent / LeaveReportStaff**：请假相关模型
- **FeedbackStudent / FeedbackStaff**：反馈相关模型
- **NotificationStudent / NotificationStaff**：通知相关模型

#### 2.1.3 表单层（Form Layer）
- **StaffForm**：教师信息表单
- **StudentForm**：学生信息表单
- **CourseForm**：课程信息表单
- **SubjectForm**：科目信息表单
- **SessionForm**：学期信息表单
- **ActivityApprovalForm**：活动审批表单
- **AdminForm**：管理员信息表单

#### 2.1.4 工具层（Utility Layer）
- **paginate_queryset**：分页工具函数
- **search_students**：学生搜索工具函数
- **search_staff**：教师搜索工具函数
- **search_activities**：活动搜索工具函数

## 3. 交互图

> **说明**：此处应插入管理员模块的交互图（序列图或协作图）。交互图应展示以下典型业务流程：
> - 添加教师流程
> - 审批请假流程
> - 活动审批流程
> - 查看统计信息流程

### 3.1 添加教师流程交互说明

**参与者**：管理员、浏览器、Django视图、表单验证器、数据库

**流程描述**：
1. 管理员在浏览器中访问"添加教师"页面
2. 浏览器发送GET请求到`add_staff`视图
3. 视图返回空表单页面
4. 管理员填写表单并提交
5. 浏览器发送POST请求到`add_staff`视图
6. 视图创建`StaffForm`实例并进行验证
7. 如果验证通过：
   - 视图检查邮箱是否已存在（调用`check_email_availability`）
   - 处理头像文件上传
   - 创建`CustomUser`实例（user_type=2）
   - 创建关联的`Staff`实例（通过信号自动创建）
   - 保存到数据库
   - 返回成功消息并重定向
8. 如果验证失败，返回错误消息并重新显示表单

### 3.2 审批请假流程交互说明

**参与者**：管理员、浏览器、Django视图、数据库、Ajax请求

**流程描述**：
1. 管理员访问"请假审批"页面
2. 浏览器发送GET请求到`view_student_leave`或`view_staff_leave`视图
3. 视图查询数据库获取所有请假申请（按创建时间倒序）
4. 视图返回请假申请列表页面
5. 管理员点击"批准"或"拒绝"按钮
6. 浏览器通过Ajax发送POST请求，包含请假ID和状态
7. 视图根据ID查找对应的请假记录
8. 视图更新请假记录的status字段（1表示批准，-1表示拒绝）
9. 视图保存到数据库
10. 视图返回JSON响应（True/False）
11. 浏览器根据响应更新页面显示

### 3.3 活动审批流程交互说明

**参与者**：管理员、浏览器、Django视图、表单验证器、数据库

**流程描述**：
1. 管理员访问"活动管理"页面
2. 浏览器发送GET请求到`admin_manage_activities`视图
3. 视图查询数据库获取所有活动（支持搜索、筛选、排序、分页）
4. 视图返回活动列表页面
5. 管理员点击某个活动进入审批页面
6. 浏览器发送GET请求到`admin_approve_activity`视图，传递活动ID
7. 视图根据ID查找活动记录
8. 视图创建`ActivityApprovalForm`实例并返回审批页面
9. 管理员填写审批意见，选择批准或拒绝，提交表单
10. 浏览器发送POST请求到`admin_approve_activity`视图
11. 视图验证表单
12. 如果验证通过：
    - 更新活动的status字段（1表示已通过，2表示已拒绝）
    - 保存审批意见到admin_reply字段
    - 保存到数据库
    - 返回成功消息并重定向到活动列表
13. 如果验证失败，返回错误消息并重新显示表单

### 3.4 查看统计信息流程交互说明

**参与者**：管理员、浏览器、Django视图、数据库、模板引擎

**流程描述**：
1. 管理员登录系统，访问管理员首页
2. 浏览器发送GET请求到`admin_home`视图
3. 视图执行多个数据库查询：
   - 统计教师总数（`Staff.objects.all().count()`）
   - 统计学生总数（`Student.objects.all().count()`）
   - 统计课程总数（`Course.objects.all().count()`）
   - 统计科目总数（`Subject.objects.all().count()`）
   - 查询所有科目及其出勤记录，生成统计数据
4. 视图将统计数据封装到context字典中
5. 视图调用模板引擎渲染`home_content.html`模板
6. 模板引擎将数据填充到模板中，生成HTML页面
7. 浏览器接收HTML响应并显示统计图表和数据

## 4. 组件图或接口定义

> **说明**：此处应插入管理员模块的组件图或接口定义图。组件图应展示：
> - 管理员模块与其他模块的关系
> - 模块内部的组件划分
> - 组件之间的依赖关系

### 4.1 模块组件划分

#### 4.1.1 视图组件（View Component）
- **用户管理视图组**：负责教师和学生的增删改查
- **基础数据管理视图组**：负责课程、科目、学期的管理
- **出勤管理视图组**：负责出勤记录的查看
- **反馈管理视图组**：负责反馈的查看和回复
- **请假管理视图组**：负责请假申请的审批
- **通知管理视图组**：负责通知的发送
- **活动管理视图组**：负责活动的审批
- **统计视图组**：负责统计信息的展示

#### 4.1.2 模型组件（Model Component）
- **用户模型组**：CustomUser、Admin、Staff、Student
- **基础数据模型组**：Course、Subject、Session
- **业务数据模型组**：Attendance、LeaveReport、Feedback、Notification
- **活动模型组**：Activity、ActivityRegistration、ActivityAttendance、ActivityFeedback

#### 4.1.3 表单组件（Form Component）
- **用户表单组**：StaffForm、StudentForm、AdminForm
- **基础数据表单组**：CourseForm、SubjectForm、SessionForm
- **活动表单组**：ActivityApprovalForm

#### 4.1.4 工具组件（Utility Component）
- **分页工具**：paginate_queryset
- **搜索工具**：search_students、search_staff、search_activities

### 4.2 接口定义

#### 4.2.1 URL路由接口
管理员模块的URL路由定义在`main_app/urls.py`中，主要路由包括：
- `/admin_home`：管理员首页
- `/add_staff`：添加教师
- `/edit_staff/<staff_id>`：编辑教师
- `/manage_staff`：管理教师列表
- `/delete_staff/<staff_id>`：删除教师
- `/add_student`：添加学生
- `/edit_student/<student_id>`：编辑学生
- `/manage_student`：管理学生列表
- `/delete_student/<student_id>`：删除学生
- `/add_course`：添加课程
- `/edit_course/<course_id>`：编辑课程
- `/manage_course`：管理课程列表
- `/delete_course/<course_id>`：删除课程
- `/add_subject`：添加科目
- `/edit_subject/<subject_id>`：编辑科目
- `/manage_subject`：管理科目列表
- `/delete_subject/<subject_id>`：删除科目
- `/add_session`：添加学期
- `/edit_session/<session_id>`：编辑学期
- `/manage_session`：管理学期列表
- `/delete_session/<session_id>`：删除学期
- `/admin_view_attendance`：查看出勤
- `/student_feedback_message`：学生反馈管理
- `/staff_feedback_message`：教师反馈管理
- `/view_student_leave`：学生请假审批
- `/view_staff_leave`：教师请假审批
- `/admin_notify_student`：向学生发送通知
- `/admin_notify_staff`：向教师发送通知
- `/admin_manage_activities`：活动管理
- `/admin_approve_activity/<activity_id>`：审批活动
- `/admin_view_profile`：个人资料管理

#### 4.2.2 视图函数接口规范
- **输入**：HTTP请求对象（request），可能包含POST数据、GET参数、文件上传
- **输出**：HTTP响应对象，通常是渲染的HTML页面或JSON响应
- **异常处理**：所有视图函数都应包含try-except块，捕获异常并返回友好的错误消息

#### 4.2.3 数据库接口
- 使用Django ORM进行数据库操作
- 使用`select_related`和`prefetch_related`优化关联查询
- 使用事务确保数据一致性

## 5. 设计模式应用

### 5.1 MVC模式
- **Model**：Django的模型类，定义数据结构和业务逻辑
- **View**：Django的视图函数，处理HTTP请求和响应
- **Controller**：Django的URL路由和视图函数的组合，控制请求流程

### 5.2 表单模式
- 使用Django Form类进行表单验证和数据清洗
- 表单验证逻辑与视图逻辑分离，提高代码可维护性

### 5.3 模板模式
- 使用Django模板系统进行页面渲染
- 模板继承机制，减少代码重复

### 5.4 信号模式
- 使用Django信号机制（post_save）自动创建关联对象
- 当创建CustomUser时，自动创建对应的Admin、Staff或Student对象

## 6. 数据流设计

### 6.1 数据流向
1. **用户输入** → 浏览器 → HTTP请求 → Django视图
2. **视图处理** → 表单验证 → 模型操作 → 数据库
3. **数据库** → 模型查询 → 视图处理 → 模板渲染 → HTTP响应 → 浏览器显示

### 6.2 数据验证流程
1. 前端表单验证（HTML5验证）
2. 后端表单验证（Django Form验证）
3. 业务逻辑验证（视图函数中的自定义验证）
4. 数据库约束验证（模型字段约束）

## 7. 性能优化设计

### 7.1 数据库查询优化
- 使用`select_related`优化一对一和一对多关联查询
- 使用`prefetch_related`优化多对多关联查询
- 避免N+1查询问题

### 7.2 分页设计
- 列表页面使用分页，每页显示10-20条记录
- 减少单次查询的数据量，提高页面加载速度

### 7.3 搜索优化
- 使用数据库索引优化搜索查询
- 支持多字段组合搜索

## 8. 安全设计

### 8.1 权限控制
- 使用中间件（middleware）进行权限验证
- 确保只有管理员可以访问管理员功能

### 8.2 数据验证
- 所有用户输入都经过表单验证
- 防止SQL注入和XSS攻击

### 8.3 文件上传安全
- 限制上传文件类型和大小
- 使用FileSystemStorage安全存储文件

