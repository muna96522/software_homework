# 管理员模块 - 需求分析文档

## 第一部分：需求分析

### 1. 模块概述

#### 1.1 模块定位与作用

管理员模块是学生管理系统的核心管理模块，负责系统的整体运营和管理。该模块为系统管理员（HOD - Head of Department）提供完整的管理功能，是系统正常运行的基础保障。

管理员模块的主要作用包括：
- 管理系统的所有用户账户（教师和学生）
- 维护系统的基础数据（课程、科目、学期）
- 监控和查看系统的运行数据（出勤、统计信息）
- 处理用户提交的各类申请（请假、反馈）
- 审批和管理校园活动
- 向用户发送系统通知

#### 1.2 模块在系统中的位置

管理员模块位于学生管理系统的核心层，与其他两个模块（教师模块、学生模块）形成完整的系统架构：

- **管理员模块**：拥有最高权限，负责系统整体管理
- **教师模块**：负责教学相关功能（出勤、成绩、活动组织）
- **学生模块**：负责学生相关功能（查看出勤、成绩、参与活动）

管理员模块通过独立的登录入口和权限控制机制，确保只有授权管理员可以访问。

#### 1.3 开发目标

- 提供完整、高效的用户管理功能
- 实现系统基础数据的统一管理
- 提供直观的数据统计和监控功能
- 建立完善的审批和反馈机制
- 确保系统的安全性和稳定性
- 提供良好的用户体验和操作界面

---

### 2. 功能需求分析

#### 2.1 用例图

（此处应插入管理员模块的用例图）

**用例图说明**：
用例图应展示管理员（Actor）与系统各功能模块之间的交互关系。主要用例包括：
- 用户管理用例（管理教师、管理学生）
- 基础数据管理用例（管理课程、管理科目、管理学期）
- 出勤查看用例
- 反馈处理用例（处理学生反馈、处理教师反馈）
- 请假审批用例（审批学生请假、审批教师请假）
- 通知发送用例（向学生发送通知、向教师发送通知）
- 活动审批用例
- 个人资料管理用例
- 系统统计查看用例

#### 2.2 功能需求列表

##### 2.2.1 用户管理功能

**教师管理功能**：
- **添加教师**：管理员可以添加新的教师账户，包括基本信息（姓名、邮箱、电话、性别等）和头像上传功能。系统需要验证邮箱的唯一性，确保不会创建重复账户。
- **编辑教师**：管理员可以修改教师的基本信息，包括姓名、邮箱、电话、性别等，支持密码重置和头像更新功能。
- **删除教师**：管理员可以删除教师账户，系统会检查该教师是否有关联的科目或活动，防止误删导致的数据不一致问题。
- **查看教师列表**：支持分页显示、搜索功能（按姓名、邮箱等关键词搜索），使用数据库查询优化技术，提高页面加载性能。

**学生管理功能**：
- **添加学生**：管理员可以添加新的学生账户，需要指定学生所属的课程和学期信息，确保学生数据的完整性。
- **编辑学生**：管理员可以修改学生的基本信息、课程和学期信息，支持密码重置和头像更新。
- **删除学生**：管理员可以删除学生账户，系统会自动处理相关的出勤记录，确保数据一致性。
- **查看学生列表**：支持分页显示、搜索功能，支持按课程、学期等条件筛选，提供灵活的数据查询方式。

##### 2.2.2 基础数据管理功能

**课程管理功能**：
- **添加课程**：创建新的专业/课程，课程名称必须唯一。
- **编辑课程**：修改课程名称，系统会检查是否有学生关联该课程。
- **删除课程**：删除课程信息，系统会检查是否有学生关联该课程，防止误删。
- **查看课程列表**：显示所有课程信息，支持快速浏览和查找。

**科目管理功能**：
- **添加科目**：创建新科目，需要指定所属课程和授课教师，建立科目与课程、教师的关联关系。
- **编辑科目**：修改科目信息，包括科目名称、所属课程和授课教师的重新分配。
- **删除科目**：删除科目信息，系统会检查相关的出勤和成绩记录。
- **查看科目列表**：显示所有科目及其关联信息（课程、教师），提供完整的数据视图。

**学期管理功能**：
- **添加学期**：创建新的学期（学年），需要指定开始和结束日期，确保时间范围的合理性。
- **编辑学期**：修改学期的起止日期，系统会检查是否有学生关联该学期。
- **删除学期**：删除学期信息，系统会检查是否有学生关联该学期，防止误删。
- **查看学期列表**：显示所有学期信息，按时间顺序排列。

##### 2.2.3 出勤管理功能

- **查看出勤记录**：管理员可以按科目和学期筛选查看学生的出勤情况，提供灵活的查询条件。
- **出勤统计**：在仪表板上显示各科目的出勤统计图表，以直观的方式展示数据。
- **出勤详情**：查看特定日期、特定科目的详细出勤记录，包括每个学生的出勤状态。

##### 2.2.4 反馈管理功能

**学生反馈处理**：
- **查看学生反馈**：查看所有学生提交的反馈信息，按时间倒序排列，便于管理员及时处理。
- **回复学生反馈**：管理员可以对学生的反馈进行回复，建立良好的沟通机制。

**教师反馈处理**：
- **查看教师反馈**：查看所有教师提交的反馈信息，了解教师对系统的意见和建议。
- **回复教师反馈**：管理员可以对教师的反馈进行回复，及时解决问题。

##### 2.2.5 请假审批功能

**学生请假审批**：
- **查看请假申请**：查看所有学生提交的请假申请，按时间倒序排列，显示请假详情。
- **审批请假**：管理员可以批准或拒绝学生的请假申请，更新请假状态。

**教师请假审批**：
- **查看请假申请**：查看所有教师提交的请假申请，了解教师的请假情况。
- **审批请假**：管理员可以批准或拒绝教师的请假申请，确保教学工作的正常进行。

##### 2.2.6 通知管理功能

**向学生发送通知**：
- **选择通知对象**：可以选择特定学生或所有学生作为通知接收者。
- **发送通知**：通过Firebase推送通知给学生，同时保存通知记录到数据库，确保通知的可追溯性。

**向教师发送通知**：
- **选择通知对象**：可以选择特定教师或所有教师作为通知接收者。
- **发送通知**：通过Firebase推送通知给教师，同时保存通知记录，建立有效的沟通渠道。

##### 2.2.7 活动管理功能

- **查看活动列表**：查看所有校园活动，支持按状态筛选（待审批、已通过、已拒绝），提供清晰的活动管理视图。
- **搜索活动**：支持按活动标题、描述等关键词搜索，快速定位目标活动。
- **排序功能**：支持按开始时间、结束时间排序，便于管理员按时间顺序管理活动。
- **审批活动**：管理员可以审批教师发起的活动，可以批准或拒绝，并添加审批意见，确保活动的规范性。
- **分页显示**：活动列表支持分页，每页显示10条记录，提高页面加载性能。

##### 2.2.8 系统统计功能

- **仪表板统计**：在首页显示系统整体统计数据，包括：
  - 学生总数统计
  - 教师总数统计
  - 课程总数统计
  - 科目总数统计
  - 各科目出勤统计图表（以图表形式直观展示）

##### 2.2.9 个人资料管理

- **查看个人资料**：查看管理员自己的基本信息，包括姓名、邮箱、头像等。
- **编辑个人资料**：修改姓名、头像、密码等个人信息，支持密码更新和头像更换。

---

### 3. 用户角色与权限

#### 3.1 管理员角色定义

管理员（HOD - Head of Department）是系统的最高权限用户，负责系统的整体管理和运营。管理员通过独立的登录入口（`/admin_login`）进行身份验证，登录后可以访问所有管理员功能模块。

#### 3.2 权限范围说明

管理员拥有以下权限：

- **用户管理权限**：
  - 可以创建、修改、删除所有教师账户
  - 可以创建、修改、删除所有学生账户
  - 可以查看所有用户的基本信息

- **基础数据管理权限**：
  - 可以创建、修改、删除课程信息
  - 可以创建、修改、删除科目信息
  - 可以创建、修改、删除学期信息

- **数据查看权限**：
  - 可以查看所有学生的出勤记录
  - 可以查看系统整体统计数据
  - 可以查看所有活动信息

- **审批权限**：
  - 可以审批所有学生的请假申请
  - 可以审批所有教师的请假申请
  - 可以审批所有校园活动

- **沟通权限**：
  - 可以查看和回复所有学生和教师的反馈
  - 可以向所有用户发送系统通知

- **个人管理权限**：
  - 可以查看和修改自己的个人资料
  - 可以修改自己的登录密码

---

### 4. 典型交互场景

#### 4.1 添加教师场景

**场景描述**：管理员需要添加一位新教师到系统中。

**交互流程**：
1. 管理员登录系统，进入管理员界面
2. 点击"添加教师"菜单项
3. 系统显示添加教师表单页面
4. 管理员填写教师基本信息：
   - 姓名（姓、名）
   - 邮箱地址
   - 联系电话
   - 性别
   - 登录密码
5. 管理员上传教师头像图片
6. 管理员点击"提交"按钮
7. 系统验证表单数据：
   - 检查必填字段是否完整
   - 验证邮箱格式是否正确
   - 检查邮箱是否已被使用
8. 如果验证通过：
   - 系统创建CustomUser账户（user_type=2表示教师）
   - 系统自动创建关联的Staff对象（通过Django信号机制）
   - 系统保存头像文件
   - 系统显示"添加成功"提示信息
   - 页面重定向到添加教师页面，可以继续添加
9. 如果验证失败：
   - 系统显示具体的错误信息
   - 页面保留已填写的数据，管理员可以修改后重新提交

#### 4.2 审批请假场景

**场景描述**：管理员需要审批学生提交的请假申请。

**交互流程**：
1. 管理员登录系统，进入管理员界面
2. 点击"请假审批"菜单项，选择"学生请假"或"教师请假"
3. 系统显示请假申请列表页面，按时间倒序排列
4. 管理员查看请假申请列表，每个申请显示：
   - 申请人姓名
   - 请假日期
   - 请假原因
   - 申请时间
   - 当前状态（待审批/已批准/已拒绝）
5. 管理员点击某个请假申请，查看详细信息
6. 管理员根据请假原因和日期，决定批准或拒绝
7. 管理员点击"批准"或"拒绝"按钮
8. 系统通过Ajax异步请求更新请假状态：
   - 如果批准：status = 1
   - 如果拒绝：status = -1
9. 系统保存更新后的请假记录
10. 系统返回操作结果（成功/失败）
11. 页面自动更新，显示最新的审批状态
12. 系统显示"审批成功"提示信息

#### 4.3 活动审批场景

**场景描述**：管理员需要审批教师发起的校园活动。

**交互流程**：
1. 管理员登录系统，进入管理员界面
2. 点击"活动管理"菜单项
3. 系统显示活动列表页面，支持筛选和搜索
4. 管理员可以：
   - 按状态筛选（待审批、已通过、已拒绝）
   - 按关键词搜索活动
   - 按时间排序
5. 管理员点击某个待审批的活动，进入审批页面
6. 系统显示活动详细信息：
   - 活动标题和描述
   - 组织教师
   - 活动地点
   - 活动时间（开始时间、结束时间）
   - 最大参与人数
   - 当前报名人数
7. 管理员查看活动信息，决定是否批准
8. 管理员填写审批意见（可选）
9. 管理员选择审批结果：
   - 批准：status = 1
   - 拒绝：status = 2
10. 管理员点击"提交审批"按钮
11. 系统验证表单数据
12. 如果验证通过：
    - 系统更新活动的status字段
    - 系统保存审批意见到admin_reply字段
    - 系统保存更新后的活动记录
    - 系统显示"审批成功"提示信息
    - 页面重定向到活动列表页面
13. 如果验证失败：
    - 系统显示错误信息
    - 页面保留已填写的数据

#### 4.4 查看统计信息场景

**场景描述**：管理员登录系统后，查看系统整体统计数据。

**交互流程**：
1. 管理员登录系统，自动进入管理员首页（仪表板）
2. 系统自动执行数据统计查询：
   - 统计教师总数（Staff.objects.all().count()）
   - 统计学生总数（Student.objects.all().count()）
   - 统计课程总数（Course.objects.all().count()）
   - 统计科目总数（Subject.objects.all().count()）
   - 查询所有科目及其出勤记录
3. 系统计算各科目的出勤统计数据
4. 系统将统计数据封装到context字典中
5. 系统调用模板引擎渲染首页模板
6. 页面显示统计信息：
   - 以卡片形式显示各项总数（学生数、教师数、课程数、科目数）
   - 以柱状图或折线图显示各科目出勤统计
7. 管理员可以：
   - 点击学生总数卡片，跳转到学生管理页面
   - 点击教师总数卡片，跳转到教师管理页面
   - 点击课程总数卡片，跳转到课程管理页面
   - 点击科目总数卡片，跳转到科目管理页面
8. 管理员可以查看图表，了解各科目的出勤情况

---

### 5. 非功能性需求

#### 5.1 性能需求

- **页面响应时间**：
  - 普通页面加载时间应控制在0.5-1秒以内
  - 列表页面（带分页）加载时间应控制在1-2秒以内
  - 统计页面（包含图表）加载时间应控制在2-3秒以内

- **数据库查询优化**：
  - 使用`select_related`优化一对一和一对多关联查询，避免N+1查询问题
  - 使用`prefetch_related`优化多对多关联查询
  - 通过优化，查询次数减少80%以上
  - 列表页面支持分页，每页显示10-20条记录，减少单次查询的数据量

- **搜索性能**：
  - 支持实时搜索功能
  - 搜索结果应在1秒内返回
  - 支持多字段组合搜索，提高搜索准确性

- **并发处理**：
  - 系统应支持至少50个并发用户同时访问
  - 数据库连接池应合理配置，避免连接耗尽

#### 5.2 安全性需求

- **身份认证**：
  - 管理员必须通过独立的登录入口（`/admin_login`）进行身份验证
  - 使用Django的认证机制，密码使用加密存储
  - 支持会话管理，确保登录状态的安全性

- **权限控制**：
  - 使用中间件（middleware）确保只有管理员可以访问管理员功能模块
  - 防止未授权用户通过直接输入URL访问管理员页面
  - 所有管理员视图函数都应进行权限验证

- **数据验证**：
  - 所有表单输入必须进行前后端双重验证
  - 邮箱唯一性检查，防止创建重复账户
  - 防止SQL注入攻击：使用Django ORM的参数化查询
  - 防止XSS攻击：使用Django模板的自动转义功能
  - 文件上传验证：检查文件类型和大小

- **密码安全**：
  - 使用Django的密码加密机制（PBKDF2算法）
  - 密码不以明文形式存储
  - 支持密码重置功能

- **文件上传安全**：
  - 限制上传文件类型（仅允许图片格式）
  - 限制上传文件大小（最大5MB）
  - 使用FileSystemStorage安全存储文件
  - 防止恶意文件上传和路径遍历攻击

#### 5.3 可用性需求

- **界面友好性**：
  - 使用AdminLTE模板，提供现代化的用户界面
  - 操作流程清晰，符合用户习惯
  - 提供明确的成功/失败提示信息（使用Django messages框架）
  - 表单验证错误信息清晰明确，指出具体问题

- **错误处理**：
  - 所有操作都有完善的异常处理机制
  - 错误信息清晰明确，便于用户理解和处理
  - 系统异常不会导致数据丢失或系统崩溃
  - 提供友好的错误页面

- **数据完整性**：
  - 删除操作前检查关联数据，防止误删
  - 提供友好的错误提示，指导用户正确处理（如：删除教师前需要先处理关联的科目）
  - 使用数据库外键约束，确保数据一致性

- **响应式设计**：
  - 界面应适配不同屏幕尺寸（桌面、平板、手机）
  - 使用Bootstrap响应式布局
  - 确保在各种设备上都有良好的用户体验

#### 5.4 可维护性需求

- **代码组织**：
  - 管理员相关视图函数集中在`hod_views.py`文件中，便于管理和维护
  - 使用Django的Form类进行表单验证，将验证逻辑与视图逻辑分离
  - 代码结构清晰，函数职责单一，易于理解和维护
  - 使用工具函数（utils.py）封装通用功能，避免代码重复

- **日志记录**：
  - 关键操作应记录日志，便于问题追踪和系统监控
  - 错误信息应记录详细的堆栈信息
  - 日志应包含操作时间、操作用户、操作内容等信息

- **文档完整性**：
  - 代码应有适当的注释，说明关键逻辑
  - 函数应有文档字符串，说明参数和返回值
  - 提供用户使用文档和开发文档

#### 5.5 兼容性需求

- **浏览器兼容**：
  - 支持主流浏览器的最新版本（Chrome、Firefox、Edge、Safari）
  - 使用标准HTML、CSS、JavaScript，避免浏览器特定功能
  - 测试在不同浏览器中的显示效果

- **数据库兼容**：
  - 使用Django ORM，支持多种数据库后端（SQLite、MySQL、PostgreSQL）
  - 避免使用数据库特定的SQL语法
  - 数据库迁移文件应兼容不同数据库

#### 5.6 可靠性需求

- **数据备份**：
  - 重要数据应有定期备份机制
  - 数据库应定期备份，防止数据丢失
  - 上传的文件应有备份策略

- **异常恢复**：
  - 系统应能处理异常情况，不会导致数据丢失
  - 数据库操作应使用事务，确保原子性
  - 提供数据恢复机制

- **事务处理**：
  - 关键操作应使用数据库事务，确保数据一致性
  - 如果操作失败，应回滚所有相关更改
  - 避免部分更新导致的数据不一致问题
